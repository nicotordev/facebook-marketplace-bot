# Servidor
PORT=3001
NODE_ENV=development

# SQLite (Drizzle + LibSQL). Ejemplo: file:local.db
DB_FILE_NAME=file:local.db

# JWT (auth). Cambiar en producción.
JWT_SECRET=your-super-secret-key-change-in-production